{
  "name": "ep_weave",
  "description": "Link all pads using Hash inspired by Squeak Swiki and Scrapbox",
  "version": "0.1.1",
  "author": {
    "name": "NII-cloud-operation",
    "url": "https://github.com/NII-cloud-operation/"
  },
  "contributors": [],
  "dependencies": {
    "minisearch": "^7.2.0",
    "node-html-parser": "^9.0.2",
    "remove-markdown": "^0.6.4",
    "uuid": "^14.0.1"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jquery": "^4.0.1",
    "@types/node": "^26.4.0",
    "eslint": "^10.9.1",
    "eslint-config-etherpad": "^4.0.4",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "build": "tsc -p tsconfig.index.json && tsc -p tsconfig.editor.json && mv lib/pad/static/js/* lib/static/js/ && rm -fr lib/pad/static && rm -rf lib/database lib/util && mv lib/pad/* lib/ && rm -fr lib/pad && cp -fr src/pad/templates lib/ && cp -fr src/pad/static/css lib/static/ && cp -fr lib/static ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NII-cloud-operation/ep_weave.git"
  }
}
